# Copy to .env and adjust. .env is gitignored.
POSTGRES_USER=whiterabbit
POSTGRES_PASSWORD=whiterabbit
POSTGRES_DB=whiterabbit

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Use a long random value in production.
WR_JWT_SECRET=dev-insecure-change-me

# Shared secret between the server and coturn for time-limited TURN credentials.
TURN_SECRET=dev-turn-secret
WR_TURN_REALM=whiterabbit
